MarkerLayer(
                                      markers: [
                                        Marker(
                                          point: LatLng(_currentLocation!.latitude, _currentLocation!.longitude),
                                          child: const Icon(
                                            Icons.location_on,
                                            size: 40.0, // Customize marker size
                                            color: Colors.red, // Customize the marker color
                                          ),
                                          width: 40,  // Customize the width
                                          height: 40, // Customize the height
                                        ),
                                      ],
                                    ),